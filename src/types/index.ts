export interface NavElementType{
    text: string;
}

export interface ButtonType{
    type?: string,
    textTheme?: string,
    text: string;
}

export interface TextBoxType{
    textTheme?:string,
    picture:string,
    tittle:string,
    bodyText?:string,
    buttonText: string,
}